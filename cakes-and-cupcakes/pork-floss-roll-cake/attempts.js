/* Pork Floss Roll Cake — attempts landing page. */
AttemptsEngine.init({
  crumbs: window.PorkFlossRollCake.crumbs,
  category: window.PorkFlossRollCake.category,
  recipeName: window.PorkFlossRollCake.recipeName,
  recipeHref: 'attempt1/index.html',
  attempts: window.PorkFlossRollCake.attempts,
});
