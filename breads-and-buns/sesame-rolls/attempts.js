/* Sesame Rolls — attempts landing page. */
AttemptsEngine.init({
  crumbs: window.SesameRolls.crumbs,
  category: window.SesameRolls.category,
  recipeName: window.SesameRolls.recipeName,
  recipeHref: 'archive1/index.html',
  attempts: window.SesameRolls.attempts,
});
